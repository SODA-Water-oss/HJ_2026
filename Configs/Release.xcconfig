//
// Release.xcconfig
// Release / App Store 构建专用配置
// 真实密钥请写入 Local.xcconfig（该文件已被 git 忽略）
//

#include "Common.xcconfig"

// MARK: - Release 专用密钥占位
SUPABASE_ANON_KEY = your-anon-key


#include "Local.xcconfig"

// Debug/Release 均不使用 Mock（连接真实后端）
USE_MOCK_SERVICES = NO
