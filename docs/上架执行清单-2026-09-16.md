# 花计2046 — 上架执行清单

> 更新日期：2026-09-16
> 状态：代码侧已就绪，剩余为密钥/账号/App Store Connect 等需本人操作项。

## 已完成（代码侧）

- [x] App 图标去除透明通道（40/58/60/80/87/120/180/1024，全部不透明 RGB）
- [x] 公开隐私政策/服务条款页：`docs/privacy.html`、`docs/terms.html`，GitHub Pages 已上线
- [x] 移除仓库内 `HJ_2026_v1.0.bundle` 并加入 `.gitignore`
- [x] `develop` 快进合并到 `main` 并推送（main 为 Pages 来源分支 + 发布分支）
- [x] Release 编译通过 + archive 通过，归档产物无 Address Sanitizer
- [x] 归档信息核验：`com.nsoft.huaji2046`，v1.0(1)，iOS 17.0，含 `PrivacyInfo.xcprivacy` 与图标

## 待办（需本人操作）

### 1. 密钥轮换（安全，最优先）

- [ ] DeepSeek 吊销旧 key `sk-d28d…`（曾进 Git 历史 `79ac75a`），新建后更新 `Configs/Local.xcconfig`
- [ ] Supabase 轮换 anon key（旧值同样在历史暴露），更新 `Configs/Local.xcconfig`
- [ ] 检查 Supabase `service_role` key 是否曾暴露，必要时轮换
- [ ] 确认 `Configs/Local.xcconfig` 已被 `.gitignore` 忽略（当前已忽略）

### 2. 后端

- [ ] Supabase 按序执行迁移 `001`–`023`（`007`/`010` 为编号跳过，无缺失）
- [ ] 部署 Edge Functions：`parse-expense`、`delete-account`、`spending-review`、`aa-bill-add-member`、`aa-validate-accounts`
- [ ] 开启 Realtime（AA 分账使用 `aa-bills-realtime` 频道）
- [ ] Auth → URL Configuration → Redirect URLs 添加 `huaji2046://reset-password`

### 3. 签名与开发者账号

- [ ] Xcode 登录开发者账号（Team `55MKGSFZJ9`），自动签名生成 Apple Distribution 证书
- [ ] 命令行当前未发现有效签名身份（`security find-identity` 返回 0）

### 4. App Store Connect

- [ ] 新建 App：名称「花计2046」、Bundle ID `com.nsoft.huaji2046`、SKU `huaji2046`、免费无 IAP
- [ ] 隐私政策 URL：`https://soda-water-oss.github.io/HJ_2026/privacy.html`
- [ ] 支持 URL、分类（主要「财务」、次要「效率」）、年龄分级 4+
- [ ] 截图：iPhone 6.7"（1290×2796）至少 6 张
- [ ] 审核备注 + 演示账号（文案见 `docs/App Store Connect 粘贴文本.md`）

### 5. 构建与上传

- [ ] 填写真实密钥后执行 `scripts/build-testflight.sh` 出 TestFlight 包
- [ ] TestFlight 回归：注册/登录 → 记账 → AI 解析 → 分析 → 注销账号 → 忘记密码
- [ ] Xcode Organizer 或 Transporter 上传，提交审核

## 验证记录（2026-09-16）

- Release `build`：`BUILD SUCCEEDED`
- Release `archive`：`ARCHIVE SUCCEEDED`，二进制无 `libclang_rt.asan_ios_dynamic`
- 归档信息：DisplayName「花计2046」、Bundle `com.nsoft.huaji2046`、版本 1.0(1)、最低 iOS 17.0
- 归档内含 `PrivacyInfo.xcprivacy`（2717 bytes）与 `Assets.car`（含 AppIcon）

## 注意事项

- 公开隐私/条款页由 GitHub Pages 从 `main` 分支 `/docs` 提供，已确认 `privacy.html`、`terms.html`、`reset-password.html` 均返回 200。
- scheme 的 `enableAddressSanitizer=YES` 是 Run 调试项，只影响 `xcodebuild build`，不影响上架用的 `archive`（已实测归档无 ASan）。
- `Configs/Info.plist` 中密钥已改为变量注入，真实值来自 `Configs/Local.xcconfig`（已 gitignore）。
