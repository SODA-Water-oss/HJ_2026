-- ============================================================
-- 011: 收入类别「投资收益」统一改为「投资」（历史数据迁移）
-- 幂等：再次执行时无匹配行，不影响
-- ============================================================

update public.records
set category = '投资'
where category = '投资收益';
